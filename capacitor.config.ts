import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tahmoures.medireminder',
  appName: 'یادآور هوشمند دارو',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
